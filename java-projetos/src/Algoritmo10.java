public class Algoritmo10 {
    void main(){
        int x,y ;
        double media;
        x = Integer.parseInt(IO.readln("Digite o primeiro número real: "));
        y = Integer.parseInt(IO.readln("Digite o segundo número real: "));
        int soma = x + y;
        media = soma;
        IO.println("Media: " + media);
    }
}
